import { html } from "lit";

export interface MakibishiConnectionHubProps {}

export const MakibishiConnectionHub = (props: MakibishiConnectionHubProps) => {
  console.log(props);
  return html`wip`;
};
